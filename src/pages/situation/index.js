import Redirect from 'umi/redirect';
import { situationDefultPage } from '@platformConfig'
// export default () => <Redirect to={`/sys/${pathname}`} />;
export default () => <Redirect to={situationDefultPage} />;